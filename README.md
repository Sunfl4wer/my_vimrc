# My basic vim configuration file

# On Windows
Copy `_vimrc` file to C:\Users\<your-user-name>\

# On Linux
Change the file to `.vimrc` and copy to the vim home folder.
To find vim home folder, in vim `:e $HOME$` then `Tab`
